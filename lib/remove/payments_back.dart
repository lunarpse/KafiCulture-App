import 'package:flutter/material.dart';

class PaymentBack extends StatelessWidget {
  const PaymentBack({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Image.asset("assets/remove/bhag.gif"),
    ));
  }
}
